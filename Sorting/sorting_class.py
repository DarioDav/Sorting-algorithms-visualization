class sorting_class:
      def __init__(self, values, colors):
        self.values = values
        self.colors = colors

      def selection_sort(self):
            n= len(self.values)
        
      def insertion_sort(self):
            n = len(self.values)