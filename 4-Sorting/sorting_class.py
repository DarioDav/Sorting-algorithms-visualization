class sorting_class:
      def __init__(self, values, colors):
            self.values = values
            self.colors = colors

      def select_sort(self):
            n = len(self.values)
      
      def insert_sort(self):
            n = len(self.values)